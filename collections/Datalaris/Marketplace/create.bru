meta {
  name: create
  type: http
  seq: 1
}

post {
  url: {{base_url}}/v1/api/admin/marketplaces
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name" : "Lazada"
  }
}

settings {
  encodeUrl: true
}
