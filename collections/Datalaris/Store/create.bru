meta {
  name: create
  type: http
  seq: 1
}

post {
  url: {{base_url}}/v1/api/admin/store
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name":"Pelangi Stationery",
    "marketplace_id":1
  }
}

settings {
  encodeUrl: true
}
