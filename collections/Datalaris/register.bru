meta {
  name: register
  type: http
  seq: 3
}

post {
  url: {{base_url}}/v1/api/register
  body: json
  auth: inherit
}

body:json {
  {
    "name":"Rofi",
    "email" : "rofituluss2@gmail.com",
    "password" :"P@ssw0rd",
    "tenant_name":null
  }
}

script:post-response {
  var jsonObject = res.getBody();
  bru.setEnvVar("token",jsonObject.data.token);
}

settings {
  encodeUrl: true
}
