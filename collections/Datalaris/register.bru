meta {
  name: register
  type: http
  seq: 3
}

post {
  url: {{base_url}}/v1/api/register
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Tes User",
    "email": "tes@mail.com",
    "password": "123",
    "tenant_name": "CV. Tes User"
  }
}

script:post-response {
  var jsonObject = res.getBody();
  bru.setEnvVar("token",jsonObject.data.token);
}

settings {
  encodeUrl: true
}
