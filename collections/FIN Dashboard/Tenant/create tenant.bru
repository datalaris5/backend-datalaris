meta {
  name: create tenant
  type: http
  seq: 1
}

post {
  url: {{base_url}}/v1/api/tenants
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name" : "Toko A"
  }
}

settings {
  encodeUrl: true
}
