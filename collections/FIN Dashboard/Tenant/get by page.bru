meta {
  name: get by page
  type: http
  seq: 2
}

post {
  url: {{base_url}}/v1/api/tenants/page
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "page":1,
    "limit":10,
    "search":""
  }
}

settings {
  encodeUrl: true
}
